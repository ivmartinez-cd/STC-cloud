services:
  # ─── Backend API ─────────────────────────────────────────────────────────────
  - type: web
    name: stc-cloud-api
    runtime: node
    region: oregon
    plan: free
    buildCommand: cd cloud && npm install && npm run build && npx knex migrate:latest --knexfile src/db/knexfile.ts
    startCommand: cd cloud && node dist/api/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: stc-cloud-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: stc-cloud-redis
          type: redis
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: PORTAL_ADMIN_USER
        value: admin
      - key: PORTAL_ADMIN_PASSWORD
        sync: false
      - key: PORTAL_ORIGIN
        sync: false
      # Cifrado at-rest de las credenciales SNMPv3 (agents.snmp_credentials).
      # NO generateValue: a diferencia de JWT_SECRET, esta clave tiene que
      # poder replicarse a mano entre entornos (restore de un pg_dump en otra
      # instancia, disaster recovery) y guardarse junto con los backups — si
      # se pierde, las credenciales SNMPv3 ya guardadas quedan ilegibles
      # (community/auth/priv v1/v2c siguen funcionando via el fallback legado).
      - key: SNMP_CREDENTIALS_KEY
        sync: false
      - key: TONER_WARN_PCT
        value: "20"
      - key: TONER_CRITICAL_PCT
        value: "10"

  # ─── Redis (cache + cola de comandos) ────────────────────────────────────────
  - type: redis
    name: stc-cloud-redis
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList: []

databases:
  # ─── NOTA: Usamos Neon en vez de Render PostgreSQL ──────────────────────────
  # Configurar DATABASE_URL manualmente en el dashboard de Render
  # con la connection string de Neon:
  #   postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/stc_cloud?sslmode=require
  #
  # Si prefieres usar Render PostgreSQL (plan free 1GB), descomentar:
  # - name: stc-cloud-db
  #   plan: free
  #   databaseName: stc_cloud
  #   user: stc_admin
